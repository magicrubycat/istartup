<div class="container">
  <%= simple_form_for @application, url: application_path(@application), method: :patch do |f| %>
    <%= f.input :content, as: :text, input_html: { value: f.object.content, :rows => 20 } %>
    <%= f.submit %>
  <% end %>
</div>
