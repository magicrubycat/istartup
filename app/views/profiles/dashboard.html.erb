<%= render 'dashboard-banner' %>

<div class="container">
  <div class="container box">
    <h4>You are interested in</h4>
    <div class="card-grid-interests">
      <div class="card-interests">
        <div class= "card-logo-interests">

        </div>
        <h6>
          <ul>
            <% @interests.each do |interest| %>
            <li> <%= interest.name %> </li>
            <% end %>
          </ul>
        </h6>
      </div>
<!--       <a class="btn btn-ghost btn-sm"></a>
      <%= link_to "Edit interests", sectors_path %> -->
    </div>
  </div>
</div>

<%= render 'dashboard-interests' %>

<%= render 'dashboard-favorites' %>

<%= render 'dashboard-applications' %>

<!--
index.scss
@import "dashboard-banner"
 -->
